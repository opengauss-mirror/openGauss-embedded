add_subdirectory(tools_dependency)
IF (ENABLE_PG_QUERY)
    add_subdirectory(intarkdb_cli)
    if(NOT MSVC)
        if (NOT ANDROID)
            add_subdirectory(intarkdb_server)
        endif()
    endif(MSVC)
ENDIF()